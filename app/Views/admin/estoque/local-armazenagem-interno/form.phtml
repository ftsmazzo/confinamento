<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
<?php $this->end() ?>

<form id="form-estoque-local-armazenagem-interno" class="form-validate" action="<?=$url_action?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf?>">
    <?php if (!empty($local->id)) : ?>
        <input type="hidden" name="id" value="<?=$local->hash()?>">
    <?php endif ?>

    <div class="card crud-form-card">
        <div class="card-body">
            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Identificação</h5>
                    <small>Subdivisão dentro de um local de estoque já cadastrado.</small>
                </div>

                <div class="row">
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="id_local_estoque" class="form-label">Local de Estoque</label>
                            <select name="id_local_estoque" id="id_local_estoque" class="form-select select2" data-search="true">
                                <option value="">Selecione</option>
                                <?php foreach ($locaisEstoque as $le) : ?>
                                    <option value="<?=$le->id?>" <?=isset($local->id_local_estoque) && (int) $local->id_local_estoque === (int) $le->id ? "selected" : ""?>><?=$le->nome?></option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="nome" class="form-label">Nome</label>
                            <input type="text" class="form-control" id="nome" name="nome" placeholder="Ex: Baia do Milho, Prateleira A" autocomplete="off" value="<?=$local->nome ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12">
                        <div class="mb-3">
                            <label for="descricao" class="form-label">Descrição</label>
                            <textarea class="form-control" id="descricao" name="descricao" rows="3" placeholder="Descrição"><?=$local->descricao ?? ""?></textarea>
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection">
                <div class="crud-section-copy mb-3">
                    <h5>Situação</h5>
                    <small>Defina se o local interno está ativo.</small>
                </div>

                <div class="row">
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="ativo" class="form-label">Ativo</label>
                            <select name="ativo" id="ativo" class="form-select" data-search="false">
                                <option value="1" <?=(!isset($local->ativo) || (int) $local->ativo === 1) ? "selected" : ""?>>Sim</option>
                                <option value="0" <?=isset($local->ativo) && (int) $local->ativo === 0 ? "selected" : ""?>>Não</option>
                            </select>
                        </div>
                    </div>
                </div>
            </div>

            <div class="mt-3 pt-4 border-top">
                <div class="d-grid gap-2 d-sm-block">
                    <button class="btn btn-primary" type="submit">Salvar</button>
                    <a href="<?=$router->route("admin.estoque.local.armazenagem.interno.index")?>" class="btn btn-white"><i class="uil uil-left"></i> Voltar</a>
                </div>
            </div>
        </div>
    </div>
</form>
