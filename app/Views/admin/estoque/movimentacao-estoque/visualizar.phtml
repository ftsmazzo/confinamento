<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
<?php $this->end() ?>

<div class="card crud-form-card mb-4">
    <div class="card-body">
        <div class="crud-subsection">
            <div class="crud-section-copy mb-3">
                <h5>Movimentação</h5>
                <small>Produto, tipo e quantidade não podem ser alterados após o registro, pois o saldo já foi atualizado. Para corrigir, exclua e registre novamente.</small>
            </div>

            <div class="row">
                <div class="col-12 col-md-3">
                    <div class="mb-3">
                        <label class="form-label">Data</label>
                        <input type="text" class="form-control" value="<?=datebr($movimentacao->data_movimentacao)?>" readonly disabled>
                    </div>
                </div>
                <div class="col-12 col-md-4">
                    <div class="mb-3">
                        <label class="form-label">Quantidade</label>
                        <input type="text" class="form-control" value="<?=number_format((float) $movimentacao->quantidade, 2, ",", ".")?>" readonly disabled>
                    </div>
                </div>
                <div class="col-12 col-md-5">
                    <div class="mb-3">
                        <label class="form-label">Motivo Original</label>
                        <input type="text" class="form-control" value="<?=$movimentacao->motivo ?? ""?>" readonly disabled>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<form id="form-estoque-movimentacao-observacao" class="form-validate" action="<?=$router->route("admin.estoque.movimentacao.update")?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf ?? ""?>">
    <input type="hidden" name="id" value="<?=$movimentacao->hash()?>">

    <div class="card crud-form-card">
        <div class="card-body">
            <div class="crud-subsection">
                <div class="crud-section-copy mb-3">
                    <h5>Motivo e Observação</h5>
                    <small>Estes são os únicos campos editáveis após o registro da movimentação.</small>
                </div>

                <div class="row">
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="motivo" class="form-label">Motivo</label>
                            <input type="text" class="form-control" id="motivo" name="motivo" placeholder="Motivo" autocomplete="off" value="<?=$movimentacao->motivo ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-8">
                        <div class="mb-3">
                            <label for="observacao" class="form-label">Observação</label>
                            <textarea class="form-control" id="observacao" name="observacao" rows="3" placeholder="Observação"><?=$movimentacao->observacao ?? ""?></textarea>
                        </div>
                    </div>
                </div>
            </div>

            <div class="mt-3 pt-4 border-top">
                <div class="d-grid gap-2 d-sm-block">
                    <button class="btn btn-primary" type="submit">Salvar</button>
                    <a href="<?=$router->route("admin.estoque.movimentacao.index")?>" class="btn btn-white"><i class="uil uil-left"></i> Voltar</a>
                </div>
            </div>
        </div>
    </div>
</form>
