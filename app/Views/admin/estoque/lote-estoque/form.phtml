<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
<?php $this->end() ?>

<form id="form-estoque-lote" class="form-validate" action="<?=$url_action?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf?>">
    <?php if (!empty($lote->id)) : ?>
        <input type="hidden" name="id" value="<?=$lote->hash()?>">
    <?php endif ?>

    <div class="card crud-form-card">
        <div class="card-body">
            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Lote de Estoque</h5>
                    <small>Rastreabilidade por lote de compra, fabricação ou validade.</small>
                </div>

                <div class="row">
                    <?php if ($id_produto) : ?>
                        <input type="hidden" name="id_produto_estoque" value="<?=$id_produto?>">
                        <?php $produtoAtual = null; foreach ($produtos as $p) { if ((int) $p->id === (int) $id_produto) { $produtoAtual = $p; break; } } ?>
                        <div class="col-12">
                            <div class="alert alert-info mb-0">Lote do produto: <strong><?=$produtoAtual->nome ?? "#$id_produto"?></strong></div>
                        </div>
                    <?php else : ?>
                        <div class="col-12 col-md-6">
                            <div class="mb-3">
                                <label for="id_produto_estoque" class="form-label">Produto</label>
                                <select name="id_produto_estoque" id="id_produto_estoque" class="form-select select2" data-search="true">
                                    <option value="">Selecione</option>
                                    <?php foreach ($produtos as $p) : ?>
                                        <option value="<?=$p->id?>" <?=isset($lote->id_produto_estoque) && (int) $lote->id_produto_estoque === (int) $p->id ? "selected" : ""?>><?=$p->nome?></option>
                                    <?php endforeach ?>
                                </select>
                            </div>
                        </div>
                    <?php endif ?>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="codigo_lote" class="form-label">Código do Lote</label>
                            <input type="text" class="form-control" id="codigo_lote" name="codigo_lote" placeholder="Código de compra/fabricação" autocomplete="off" value="<?=$lote->codigo_lote ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="data_validade" class="form-label">Validade</label>
                            <input type="date" class="form-control" id="data_validade" name="data_validade" autocomplete="off" value="<?=!empty($lote->data_validade) ? dateus($lote->data_validade) : ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="quantidade_atual" class="form-label">Quantidade Atual</label>
                            <input type="text" class="form-control input-decimal" id="quantidade_atual" name="quantidade_atual" placeholder="0,0" autocomplete="off" value="<?=isset($lote->quantidade_atual) ? number_format((float) $lote->quantidade_atual, 2, ",", ".") : ""?>">
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection">
                <div class="crud-section-copy mb-3">
                    <h5>Observações</h5>
                    <small>Informações livres para apoio operacional.</small>
                </div>

                <div class="row">
                    <div class="col-12">
                        <div class="mb-3">
                            <textarea class="form-control" id="observacao" name="observacao" rows="3" placeholder="Observação"><?=$lote->observacao ?? ""?></textarea>
                        </div>
                    </div>
                </div>
            </div>

            <div class="mt-3 pt-4 border-top">
                <div class="d-grid gap-2 d-sm-block">
                    <button class="btn btn-primary" type="submit">Salvar</button>
                    <a href="<?=$url_voltar?>" class="btn btn-white"><i class="uil uil-left"></i> Voltar</a>
                </div>
            </div>
        </div>
    </div>
</form>
