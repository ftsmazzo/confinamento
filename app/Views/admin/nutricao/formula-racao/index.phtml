<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-buttons") ?>
    <?php if (!empty($permissao["inserir"])) : ?>
        <a class="btn btn-primary" href="<?=$router->route("admin.nutricao.formula.racao.novo")?>"><i class="fa fa-plus fa-fw"></i> Nova Fórmula</a>
    <?php endif ?>
<?php $this->end() ?>

<div class="card crud-table-card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover table-sm table-datatable table-datatable-alt crud-table">
                <thead>
                    <tr>
                        <th>Nome</th>
                        <th>Tipo</th>
                        <th>Fase</th>
                        <th>Composição</th>
                        <th>% MS</th>
                        <th>Ativo</th>
                        <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                            <th class="text-end actions-column"></th>
                        <?php endif ?>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($dados as $d) : ?>
                        <tr>
                            <td>
                                <?php if ($permissao["editar"]) : ?>
                                    <a class="edit" href="<?=$router->route("admin.nutricao.formula.racao.editar", ["id" => $d->hash])?>"><?=$d->nome?></a>
                                <?php else : ?>
                                    <span><?=$d->nome?></span>
                                <?php endif ?>
                            </td>
                            <td><?=$d->tipo_dieta_descricao ?: "-"?></td>
                            <td><?=$d->fase_descricao ?: "-"?></td>
                            <td>
                                <?php if ((float) $d->total_percentual === 100.0) : ?>
                                    <span class="badge filled-outlined bg-success"><?=number_format((float) $d->total_percentual, 2, ",", ".")?>%</span>
                                <?php elseif ((float) $d->total_percentual > 0) : ?>
                                    <span class="badge filled-outlined bg-warning" data-tooltip="true" title="Composição não fecha em 100%"><?=number_format((float) $d->total_percentual, 2, ",", ".")?>%</span>
                                <?php else : ?>
                                    <span class="badge filled-outlined bg-secondary">Sem itens</span>
                                <?php endif ?>
                            </td>
                            <td>
                                <?php if ($d->materia_seca !== null) : ?>
                                    <?=number_format((float) $d->materia_seca, 2, ",", ".")?>%
                                <?php else : ?>
                                    <span class="text-muted">—</span>
                                <?php endif ?>
                            </td>
                            <td><?=!empty($d->ativo) ? '<span class="badge filled-outlined bg-success">Sim</span>' : '<span class="badge filled-outlined bg-secondary">Não</span>'?></td>
                            <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                                <td class="text-end actions-column">
                                    <div class="table-actions">
                                        <?php if ($permissao["editar"]) : ?>
                                            <a class="table-action-btn" data-tooltip="true" title="Editar" href="<?=$router->route("admin.nutricao.formula.racao.editar", ["id" => $d->hash])?>"><i class="uil-edit-alt"></i></a>
                                        <?php endif ?>
                                        <?php if ($permissao["excluir"]) : ?>
                                            <button type="button" class="table-action-btn table-action-danger" data-tooltip="true" title="Excluir fórmula" onclick="Delete('nutricao/formulas-racao/delete', '<?=$d->hash?>')"><i class="uil uil-trash"></i></button>
                                        <?php endif ?>
                                    </div>
                                </td>
                            <?php endif ?>
                        </tr>
                    <?php endforeach ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
