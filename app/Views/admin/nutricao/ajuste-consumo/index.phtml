<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
    <?php if ($lote) : ?>
        <a class="btn btn-white btn-sm" href="<?=$router->route("admin.manejo.lote.editar", ["id" => $lote->hash()])?>"><i class="uil uil-left"></i> Voltar ao Lote</a>
    <?php endif ?>
<?php $this->end() ?>

<?php $this->start("page-buttons") ?>
    <?php if (!empty($permissao["inserir"])) : ?>
        <a class="btn btn-primary" href="<?=$router->route("admin.nutricao.ajuste.consumo.novo", array_filter(["id_lote" => $lote->id ?? null]))?>"><i class="fa fa-plus fa-fw"></i> Novo Ajuste</a>
    <?php endif ?>
<?php $this->end() ?>

<div class="card crud-table-card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover table-sm table-datatable table-datatable-alt crud-table">
                <thead>
                    <tr>
                        <?php if (!$lote) : ?>
                            <th>Lote</th>
                        <?php endif ?>
                        <th>Data</th>
                        <th>Leitura de Cocho</th>
                        <th>Percentual</th>
                        <th>Qtd. Ajustada</th>
                        <th>Motivo</th>
                        <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                            <th class="text-end actions-column"></th>
                        <?php endif ?>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($dados as $d) : ?>
                        <tr>
                            <?php if (!$lote) : ?>
                                <td><?=$d->vinculo_print?></td>
                            <?php endif ?>
                            <td><?=datebr($d->data_ajuste)?></td>
                            <td><?=$d->leitura_escore_label ?: "-"?></td>
                            <td><?=$d->percentual_ajuste !== null ? number_format((float) $d->percentual_ajuste, 2, ",", ".") . "%" : "-"?></td>
                            <td><?=$d->quantidade_ajustada !== null ? number_format((float) $d->quantidade_ajustada, 2, ",", ".") : "-"?></td>
                            <td><?=$d->motivo ?: "-"?></td>
                            <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                                <td class="text-end actions-column">
                                    <div class="table-actions">
                                        <?php if ($permissao["editar"]) : ?>
                                            <a class="table-action-btn" data-tooltip="true" title="Editar" href="<?=$router->route("admin.nutricao.ajuste.consumo.editar", ["id" => $d->hash])?>"><i class="uil-edit-alt"></i></a>
                                        <?php endif ?>
                                        <?php if ($permissao["excluir"]) : ?>
                                            <button type="button" class="table-action-btn table-action-danger" data-tooltip="true" title="Excluir ajuste" onclick="Delete('nutricao/ajustes-consumo/delete', '<?=$d->hash?>')"><i class="uil uil-trash"></i></button>
                                        <?php endif ?>
                                    </div>
                                </td>
                            <?php endif ?>
                        </tr>
                    <?php endforeach ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
