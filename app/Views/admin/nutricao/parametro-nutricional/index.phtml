<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-buttons") ?>
    <?php if (!empty($permissao["inserir"])) : ?>
        <button type="button" class="btn btn-primary" data-modal-ajax="<?=$router->route("admin.nutricao.parametro.nutricional.novo")?>" data-modal-target="#modal-parametro-nutricional" data-modal-title="Novo Parâmetro"><i class="fa fa-plus fa-fw"></i> Novo Parâmetro</button>
    <?php endif ?>
<?php $this->end() ?>

<div class="card crud-table-card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover table-sm table-datatable table-datatable-alt crud-table">
                <thead>
                    <tr>
                        <th>Nome</th>
                        <th>Unidade</th>
                        <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                            <th class="text-end actions-column"></th>
                        <?php endif ?>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($dados as $d) : ?>
                        <tr>
                            <td>
                                <?php if ($permissao["editar"]) : ?>
                                    <a class="edit" href="<?=$router->route("admin.nutricao.parametro.nutricional.editar", ["id" => $d->hash])?>" data-modal-ajax="<?=$router->route("admin.nutricao.parametro.nutricional.editar", ["id" => $d->hash])?>" data-modal-target="#modal-parametro-nutricional" data-modal-title="Editar Parâmetro"><?=$d->nome?></a>
                                <?php else : ?>
                                    <span><?=$d->nome?></span>
                                <?php endif ?>
                            </td>
                            <td><?=$d->unidade_medida ?: "-"?></td>
                            <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                                <td class="text-end actions-column">
                                    <div class="table-actions">
                                        <?php if ($permissao["editar"]) : ?>
                                            <button type="button" class="table-action-btn" data-tooltip="true" title="Editar" data-modal-ajax="<?=$router->route("admin.nutricao.parametro.nutricional.editar", ["id" => $d->hash])?>" data-modal-target="#modal-parametro-nutricional" data-modal-title="Editar Parâmetro"><i class="uil-edit-alt"></i></button>
                                        <?php endif ?>
                                        <?php if ($permissao["excluir"]) : ?>
                                            <button type="button" class="table-action-btn table-action-danger" data-tooltip="true" title="Excluir parâmetro" onclick="Delete('nutricao/parametros-nutricionais/delete', '<?=$d->hash?>')"><i class="uil uil-trash"></i></button>
                                        <?php endif ?>
                                    </div>
                                </td>
                            <?php endif ?>
                        </tr>
                    <?php endforeach ?>
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="modal fade" id="modal-parametro-nutricional" tabindex="-1" aria-hidden="true" data-bs-backdrop="static">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title"></h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fechar"></button>
            </div>
            <div class="modal-body"></div>
            <div class="modal-footer mt-3">
                <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancelar</button>
                <button type="submit" class="btn btn-primary" form="form-parametro-nutricional"><i class="uil uil-check me-1"></i>Salvar</button>
            </div>
        </div>
    </div>
</div>

<?php $this->start("endScripts") ?>
<script src="<?=asset("admin/js/parametro-nutricional-form.js")?>"></script>
<?php $this->end() ?>
