<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
<?php $this->end() ?>

<form id="form-nutricao-programacao-trato" class="form-validate" action="<?=$url_action?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf?>">
    <?php if (!empty($programacao->id)) : ?>
        <input type="hidden" name="id" value="<?=$programacao->hash()?>">
    <?php endif ?>

    <div class="card crud-form-card">
        <div class="card-body">
            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Programação</h5>
                    <small>Planejamento do trato a ser fornecido ao lote.</small>
                </div>

                <div class="row">
                    <?php if ($id_lote) : ?>
                        <input type="hidden" name="id_lote" value="<?=$id_lote?>">
                        <?php $loteAtual = null; foreach ($lotes as $l) { if ((int) $l->id === (int) $id_lote) { $loteAtual = $l; break; } } ?>
                        <div class="col-12">
                            <div class="alert alert-info mb-0">Programando trato do lote: <strong><?=$loteAtual->nome ?? "#$id_lote"?></strong></div>
                        </div>
                    <?php else : ?>
                        <div class="col-12 col-md-6">
                            <div class="mb-3">
                                <label for="id_lote" class="form-label">Lote</label>
                                <select name="id_lote" id="id_lote" class="form-select select2" data-search="true">
                                    <option value="">Selecione</option>
                                    <?php foreach ($lotes as $l) : ?>
                                        <option value="<?=$l->id?>" <?=isset($programacao->id_lote) && (int) $programacao->id_lote === (int) $l->id ? "selected" : ""?>><?=$l->nome?> (<?=$l->codigo?>)</option>
                                    <?php endforeach ?>
                                </select>
                            </div>
                        </div>
                    <?php endif ?>
                    <div class="col-12 col-md-3">
                        <div class="mb-3">
                            <label for="data_programacao" class="form-label">Data</label>
                            <input type="date" class="form-control" id="data_programacao" name="data_programacao" autocomplete="off" value="<?=!empty($programacao->data_programacao) ? dateus($programacao->data_programacao) : ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-3">
                        <div class="mb-3">
                            <label for="turno" class="form-label">Turno</label>
                            <select name="turno" id="turno" class="form-select" data-search="false">
                                <option value="">Selecione</option>
                                <?php foreach (["MANHA" => "Manhã", "TARDE" => "Tarde", "NOITE" => "Noite"] as $valor => $label) : ?>
                                    <option value="<?=$valor?>" <?=isset($programacao->turno) && $programacao->turno === $valor ? "selected" : ""?>><?=$label?></option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="id_curral" class="form-label">Curral</label>
                            <select name="id_curral" id="id_curral" class="form-select select2" data-search="true">
                                <option value="">Selecione</option>
                                <?php foreach ($currais as $c) : ?>
                                    <option value="<?=$c->id?>" <?=isset($programacao->id_curral) && (int) $programacao->id_curral === (int) $c->id ? "selected" : ""?>><?=$c->nome?></option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="id_formula_racao" class="form-label">Fórmula de Ração</label>
                            <select name="id_formula_racao" id="id_formula_racao" class="form-select select2" data-search="true">
                                <option value="">Selecione</option>
                                <?php foreach ($formulas as $formula) : ?>
                                    <option value="<?=$formula->id?>" <?=isset($programacao->id_formula_racao) && (int) $programacao->id_formula_racao === (int) $formula->id ? "selected" : ""?>><?=$formula->nome?></option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-2">
                        <div class="mb-3">
                            <label for="quantidade_prevista" class="form-label">Qtd. Prevista</label>
                            <input type="text" class="form-control input-decimal" id="quantidade_prevista" name="quantidade_prevista" placeholder="0,00" autocomplete="off" value="<?=isset($programacao->quantidade_prevista) ? number_format((float) $programacao->quantidade_prevista, 2, ",", ".") : ""?>">
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection">
                <div class="crud-section-copy mb-3">
                    <h5>Observações</h5>
                    <small>Informações livres para apoio operacional.</small>
                </div>

                <div class="row">
                    <div class="col-12">
                        <div class="mb-3">
                            <textarea class="form-control" id="observacao" name="observacao" rows="3" placeholder="Observação"><?=$programacao->observacao ?? ""?></textarea>
                        </div>
                    </div>
                </div>
            </div>

            <div class="mt-3 pt-4 border-top">
                <div class="d-grid gap-2 d-sm-block">
                    <button class="btn btn-primary" type="submit">Salvar</button>
                    <a href="<?=$url_voltar?>" class="btn btn-white"><i class="uil uil-left"></i> Voltar</a>
                </div>
            </div>
        </div>
    </div>
</form>
