<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
    <a class="btn btn-white btn-sm" href="<?=$url_voltar?>"><i class="uil uil-left"></i> Voltar</a>
<?php $this->end() ?>

<form id="form-gerar-previsao-trato" class="form-validate" action="<?=$url_action?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf?>">

    <div class="card crud-form-card">
        <div class="card-body">
            <div class="alert alert-info">
                O motor calcula: <strong>dias</strong> → <strong>peso projetado (GMD)</strong> →
                <strong>consumo MS (%PV)</strong> → <strong>MN (% MS da fórmula)</strong> →
                <strong>4 turnos</strong>. Cada lote precisa de entrada (qtd/peso/data), troca de dieta e % MS na fórmula.
            </div>

            <div class="row">
                <div class="col-12 col-md-4">
                    <div class="mb-3">
                        <label for="data_programacao" class="form-label">Data</label>
                        <input type="date" class="form-control" id="data_programacao" name="data_programacao" value="<?=$data_programacao?>" required>
                    </div>
                </div>
                <div class="col-12 col-md-5">
                    <div class="mb-3">
                        <label for="id_lote" class="form-label">Lote (opcional)</label>
                        <select name="id_lote" id="id_lote" class="form-select select2" data-search="true">
                            <option value="">Todos os lotes ativos</option>
                            <?php foreach ($lotes as $l) : ?>
                                <option value="<?=$l->id?>" <?=!empty($id_lote) && (int) $id_lote === (int) $l->id ? "selected" : ""?>><?=$l->nome?><?=!empty($l->codigo) ? " ({$l->codigo})" : ""?></option>
                            <?php endforeach ?>
                        </select>
                    </div>
                </div>
                <div class="col-12 col-md-3">
                    <div class="mb-3">
                        <label class="form-label d-block">&nbsp;</label>
                        <div class="form-check mt-2">
                            <input class="form-check-input" type="checkbox" value="1" id="substituir" name="substituir" checked>
                            <label class="form-check-label" for="substituir">Substituir previstos do dia</label>
                        </div>
                    </div>
                </div>
            </div>

            <div class="mt-3 pt-4 border-top">
                <div class="d-grid gap-2 d-sm-block">
                    <button class="btn btn-primary" type="submit"><i class="fa fa-calculator fa-fw"></i> Gerar previstos</button>
                    <a href="<?=$url_voltar?>" class="btn btn-white">Cancelar</a>
                </div>
            </div>
        </div>
    </div>
</form>
