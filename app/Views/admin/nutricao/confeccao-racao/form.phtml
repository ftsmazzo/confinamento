<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
<?php $this->end() ?>

<form id="form-nutricao-confeccao-racao" class="form-validate" action="<?=$url_action?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf?>">

    <div class="card crud-form-card">
        <div class="card-body">
            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Confecção</h5>
                    <small>Ao salvar, o estoque dos ingredientes da fórmula é baixado automaticamente conforme o percentual de cada um.</small>
                </div>

                <div class="row">
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="id_formula_racao" class="form-label">Fórmula de Ração</label>
                            <select name="id_formula_racao" id="id_formula_racao" class="form-select select2" data-search="true">
                                <option value="">Selecione</option>
                                <?php foreach ($formulas as $formula) : ?>
                                    <option value="<?=$formula->id?>"><?=$formula->nome?></option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-3">
                        <div class="mb-3">
                            <label for="data_confeccao" class="form-label">Data</label>
                            <input type="date" class="form-control" id="data_confeccao" name="data_confeccao" autocomplete="off">
                        </div>
                    </div>
                    <div class="col-12 col-md-3">
                        <div class="mb-3">
                            <label for="quantidade_prevista" class="form-label">Qtd. Prevista</label>
                            <input type="text" class="form-control input-decimal" id="quantidade_prevista" name="quantidade_prevista" placeholder="0,00" autocomplete="off">
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="quantidade_real" class="form-label">Qtd. Real Produzida</label>
                            <input type="text" class="form-control input-decimal" id="quantidade_real" name="quantidade_real" placeholder="0,00" autocomplete="off">
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-12">
                        <div class="mb-3">
                            <label for="observacao" class="form-label">Observação</label>
                            <textarea class="form-control" id="observacao" name="observacao" rows="3" placeholder="Observação"></textarea>
                        </div>
                    </div>
                </div>
            </div>

            <div class="mt-3 pt-4 border-top">
                <div class="d-grid gap-2 d-sm-block">
                    <button class="btn btn-primary" type="submit">Salvar e Baixar Estoque</button>
                    <a href="<?=$router->route("admin.nutricao.confeccao.racao.index")?>" class="btn btn-white"><i class="uil uil-left"></i> Voltar</a>
                </div>
            </div>
        </div>
    </div>
</form>
