<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
<?php $this->end() ?>

<div class="card crud-form-card mb-4">
    <div class="card-body">
        <div class="crud-subsection mb-4">
            <div class="crud-section-copy mb-3">
                <h5>Confecção</h5>
                <small>Quantidade e fórmula não podem ser alteradas após o registro, pois o estoque já foi baixado. Para corrigir, exclua e registre novamente.</small>
            </div>

            <div class="row">
                <div class="col-12 col-md-4">
                    <div class="mb-3">
                        <label class="form-label">Fórmula</label>
                        <input type="text" class="form-control" value="<?=$confeccao->id_formula_racao_nome ?? ""?>" readonly disabled>
                    </div>
                </div>
                <div class="col-12 col-md-3">
                    <div class="mb-3">
                        <label class="form-label">Data</label>
                        <input type="text" class="form-control" value="<?=datebr($confeccao->data_confeccao)?>" readonly disabled>
                    </div>
                </div>
                <div class="col-12 col-md-2">
                    <div class="mb-3">
                        <label class="form-label">Qtd. Prevista</label>
                        <input type="text" class="form-control" value="<?=$confeccao->quantidade_prevista !== null ? number_format((float) $confeccao->quantidade_prevista, 2, ",", ".") : "-"?>" readonly disabled>
                    </div>
                </div>
                <div class="col-12 col-md-3">
                    <div class="mb-3">
                        <label class="form-label">Qtd. Real Produzida</label>
                        <input type="text" class="form-control" value="<?=number_format((float) $confeccao->quantidade_real, 2, ",", ".")?>" readonly disabled>
                    </div>
                </div>
            </div>
        </div>

        <div class="crud-subsection">
            <div class="crud-section-copy mb-3">
                <h5>Consumo de Ingredientes</h5>
                <small>Snapshot do percentual da fórmula e quantidade consumida no momento da confecção.</small>
            </div>

            <div class="table-responsive">
                <table class="table table-sm table-striped">
                    <thead>
                        <tr>
                            <th>Ingrediente</th>
                            <th>Percentual da Fórmula</th>
                            <th>Quantidade Consumida</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($itens as $item) : ?>
                            <tr>
                                <td><?=$item->ingrediente_nome ?: "-"?></td>
                                <td><?=number_format((float) $item->percentual_formula, 2, ",", ".")?>%</td>
                                <td><?=number_format((float) $item->quantidade_consumida, 2, ",", ".")?></td>
                            </tr>
                        <?php endforeach ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<form id="form-nutricao-confeccao-racao-observacao" class="form-validate" action="<?=$router->route("admin.nutricao.confeccao.racao.update")?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf ?? ""?>">
    <input type="hidden" name="id" value="<?=$confeccao->hash()?>">

    <div class="card crud-form-card">
        <div class="card-body">
            <div class="crud-subsection">
                <div class="crud-section-copy mb-3">
                    <h5>Observação</h5>
                    <small>Este é o único campo editável após o registro da confecção.</small>
                </div>

                <div class="row">
                    <div class="col-12">
                        <div class="mb-3">
                            <textarea class="form-control" name="observacao" rows="3" placeholder="Observação"><?=$confeccao->observacao ?? ""?></textarea>
                        </div>
                    </div>
                </div>
            </div>

            <div class="mt-3 pt-4 border-top">
                <div class="d-grid gap-2 d-sm-block">
                    <button class="btn btn-primary" type="submit">Salvar Observação</button>
                    <a href="<?=$router->route("admin.nutricao.confeccao.racao.index")?>" class="btn btn-white"><i class="uil uil-left"></i> Voltar</a>
                </div>
            </div>
        </div>
    </div>
</form>
