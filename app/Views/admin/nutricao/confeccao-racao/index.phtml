<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-buttons") ?>
    <?php if (!empty($permissao["inserir"])) : ?>
        <a class="btn btn-primary" href="<?=$router->route("admin.nutricao.confeccao.racao.novo")?>"><i class="fa fa-plus fa-fw"></i> Nova Confecção</a>
    <?php endif ?>
<?php $this->end() ?>

<div class="card crud-table-card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover table-sm table-datatable table-datatable-alt crud-table">
                <thead>
                    <tr>
                        <th>Data</th>
                        <th>Fórmula</th>
                        <th>Operador</th>
                        <th>Qtd. Prevista</th>
                        <th>Qtd. Real</th>
                        <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                            <th class="text-end actions-column"></th>
                        <?php endif ?>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($dados as $d) : ?>
                        <tr>
                            <td><?=datebr($d->data_confeccao)?></td>
                            <td><span class="badge filled-outlined bg-secondary"><?=$d->formula_nome ?: "-"?></span></td>
                            <td><?=$d->operador_nome ?: "-"?></td>
                            <td><?=$d->quantidade_prevista !== null ? number_format((float) $d->quantidade_prevista, 2, ",", ".") : "-"?></td>
                            <td><?=number_format((float) $d->quantidade_real, 2, ",", ".")?></td>
                            <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                                <td class="text-end actions-column">
                                    <div class="table-actions">
                                        <?php if ($permissao["editar"]) : ?>
                                            <a class="table-action-btn" data-tooltip="true" title="Visualizar / Editar Observação" href="<?=$router->route("admin.nutricao.confeccao.racao.editar", ["id" => $d->hash])?>"><i class="uil-edit-alt"></i></a>
                                        <?php endif ?>
                                        <?php if ($permissao["excluir"]) : ?>
                                            <button type="button" class="table-action-btn table-action-danger" data-tooltip="true" title="Excluir confecção (estorna estoque)" onclick="Delete('nutricao/confeccoes-racao/delete', '<?=$d->hash?>')"><i class="uil uil-trash"></i></button>
                                        <?php endif ?>
                                    </div>
                                </td>
                            <?php endif ?>
                        </tr>
                    <?php endforeach ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
