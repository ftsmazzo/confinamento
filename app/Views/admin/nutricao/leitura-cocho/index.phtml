<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
    <?php if ($lote) : ?>
        <a class="btn btn-white btn-sm" href="<?=$router->route("admin.manejo.lote.editar", ["id" => $lote->hash()])?>"><i class="uil uil-left"></i> Voltar ao Lote</a>
    <?php endif ?>
<?php $this->end() ?>

<?php $this->start("page-buttons") ?>
    <?php if (!empty($permissao["inserir"])) : ?>
        <a class="btn btn-primary" href="<?=$router->route("admin.nutricao.leitura.cocho.novo", array_filter(["id_lote" => $lote->id ?? null]))?>"><i class="fa fa-plus fa-fw"></i> Nova Leitura</a>
    <?php endif ?>
<?php $this->end() ?>

<?php
    $qsBase = array_filter([
        "id_lote" => $lote->id ?? null,
        "data" => $data_filtro,
        "turno" => $turno_filtro ?: null,
    ]);
?>

<div class="card crud-form-card mb-3">
    <div class="card-body">
        <form method="GET" action="<?=$router->route("admin.nutricao.leitura.cocho.index")?>" class="row g-2 align-items-end">
            <?php if (!empty($lote->id)) : ?>
                <input type="hidden" name="id_lote" value="<?=$lote->id?>">
            <?php endif ?>
            <div class="col-12 col-md-3">
                <label class="form-label">Visão</label>
                <select name="visao" class="form-select form-select-sm">
                    <option value="dia" <?= $visao === "dia" ? "selected" : ""?>>Dia (manhã → noite)</option>
                    <option value="historico" <?= $visao === "historico" ? "selected" : ""?>>Histórico</option>
                </select>
            </div>
            <div class="col-6 col-md-3">
                <label class="form-label">Data</label>
                <input type="date" name="data" class="form-control form-control-sm" value="<?=$data_filtro?>" <?= $visao === "historico" ? "disabled" : ""?>>
            </div>
            <div class="col-6 col-md-3">
                <label class="form-label">Turno</label>
                <select name="turno" class="form-select form-select-sm" <?= $visao === "historico" ? "disabled" : ""?>>
                    <option value="">Todos</option>
                    <?php foreach ($turnos_label as $valor => $label) : ?>
                        <option value="<?=$valor?>" <?= $turno_filtro === $valor ? "selected" : ""?>><?=$label?></option>
                    <?php endforeach ?>
                </select>
            </div>
            <div class="col-12 col-md-3">
                <button type="submit" class="btn btn-primary btn-sm w-100">Filtrar</button>
            </div>
        </form>
    </div>
</div>

<?php if ($visao === "dia") : ?>
    <?php foreach ($turnos_label as $turno => $label) : ?>
        <?php $itens = $por_turno[$turno] ?? []; ?>
        <div class="card crud-table-card mb-3">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h5 class="mb-0"><?=$label?></h5>
                    <span class="badge filled-outlined bg-secondary"><?=count($itens)?> leitura(s)</span>
                </div>
                <?php if (!$itens) : ?>
                    <p class="text-muted mb-0">Nenhuma leitura neste turno em <?=datebr($data_filtro)?>.</p>
                <?php else : ?>
                    <div class="table-responsive">
                        <table class="table table-striped table-hover table-sm crud-table mb-0">
                            <thead>
                                <tr>
                                    <?php if (!$lote) : ?><th>Lote</th><?php endif ?>
                                    <th>Curral</th>
                                    <th>Escore</th>
                                    <th>Foto</th>
                                    <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                                        <th class="text-end actions-column"></th>
                                    <?php endif ?>
                                </tr>
                            </thead>
                            <tbody>
                                <?php foreach ($itens as $d) : ?>
                                    <tr>
                                        <?php if (!$lote) : ?><td><?=$d->vinculo_print?></td><?php endif ?>
                                        <td><?=$d->curral_nome ?: "-"?></td>
                                        <td>
                                            <?php if ($d->escore_valor === null) : ?>
                                                <span class="badge filled-outlined bg-light text-dark">Sem nota</span>
                                            <?php else : ?>
                                                <span class="badge filled-outlined bg-secondary"><?=$d->escore_valor?> - <?=$d->escore_label?></span>
                                            <?php endif ?>
                                        </td>
                                        <td>
                                            <?php if ($d->foto_url) : ?>
                                                <a href="<?=$d->foto_url?>" target="_blank" rel="noopener">Ver foto</a>
                                            <?php else : ?>
                                                -
                                            <?php endif ?>
                                        </td>
                                        <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                                            <td class="text-end actions-column">
                                                <div class="table-actions">
                                                    <?php if ($permissao["editar"]) : ?>
                                                        <a class="table-action-btn" data-tooltip="true" title="Editar" href="<?=$router->route("admin.nutricao.leitura.cocho.editar", ["id" => $d->hash])?>"><i class="uil-edit-alt"></i></a>
                                                    <?php endif ?>
                                                    <?php if ($permissao["excluir"]) : ?>
                                                        <button type="button" class="table-action-btn table-action-danger" data-tooltip="true" title="Excluir leitura" onclick="Delete('nutricao/leituras-cocho/delete', '<?=$d->hash?>')"><i class="uil uil-trash"></i></button>
                                                    <?php endif ?>
                                                </div>
                                            </td>
                                        <?php endif ?>
                                    </tr>
                                <?php endforeach ?>
                            </tbody>
                        </table>
                    </div>
                <?php endif ?>
            </div>
        </div>
    <?php endforeach ?>

    <?php if (!empty($por_turno[""])) : ?>
        <div class="card crud-table-card mb-3">
            <div class="card-body">
                <h5 class="mb-3">Sem turno informado</h5>
                <div class="table-responsive">
                    <table class="table table-sm crud-table mb-0">
                        <tbody>
                            <?php foreach ($por_turno[""] as $d) : ?>
                                <tr>
                                    <?php if (!$lote) : ?><td><?=$d->vinculo_print?></td><?php endif ?>
                                    <td><?=$d->curral_nome ?: "-"?></td>
                                    <td><?=$d->escore_valor === null ? "Sem nota" : ($d->escore_valor . " - " . $d->escore_label)?></td>
                                    <td><?php if ($d->foto_url) : ?><a href="<?=$d->foto_url?>" target="_blank">Foto</a><?php else : ?>-<?php endif ?></td>
                                </tr>
                            <?php endforeach ?>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    <?php endif ?>
<?php else : ?>
    <div class="card crud-table-card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover table-sm table-datatable table-datatable-alt crud-table">
                    <thead>
                        <tr>
                            <?php if (!$lote) : ?>
                                <th>Lote</th>
                            <?php endif ?>
                            <th>Data</th>
                            <th>Turno</th>
                            <th>Curral</th>
                            <th>Escore</th>
                            <th>Foto</th>
                            <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                                <th class="text-end actions-column"></th>
                            <?php endif ?>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($dados as $d) : ?>
                            <tr>
                                <?php if (!$lote) : ?>
                                    <td><?=$d->vinculo_print?></td>
                                <?php endif ?>
                                <td><?=datebr($d->data_leitura)?></td>
                                <td><?=$d->turno_label?></td>
                                <td><?=$d->curral_nome ?: "-"?></td>
                                <td>
                                    <?php if ($d->escore_valor === null) : ?>
                                        <span class="badge filled-outlined bg-light text-dark">Sem nota</span>
                                    <?php else : ?>
                                        <span class="badge filled-outlined bg-secondary"><?=$d->escore_valor?> - <?=$d->escore_label?></span>
                                    <?php endif ?>
                                </td>
                                <td>
                                    <?php if ($d->foto_url) : ?>
                                        <a href="<?=$d->foto_url?>" target="_blank" rel="noopener">Ver</a>
                                    <?php else : ?>
                                        -
                                    <?php endif ?>
                                </td>
                                <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                                    <td class="text-end actions-column">
                                        <div class="table-actions">
                                            <?php if ($permissao["editar"]) : ?>
                                                <a class="table-action-btn" data-tooltip="true" title="Editar" href="<?=$router->route("admin.nutricao.leitura.cocho.editar", ["id" => $d->hash])?>"><i class="uil-edit-alt"></i></a>
                                            <?php endif ?>
                                            <?php if ($permissao["excluir"]) : ?>
                                                <button type="button" class="table-action-btn table-action-danger" data-tooltip="true" title="Excluir leitura" onclick="Delete('nutricao/leituras-cocho/delete', '<?=$d->hash?>')"><i class="uil uil-trash"></i></button>
                                            <?php endif ?>
                                        </div>
                                    </td>
                                <?php endif ?>
                            </tr>
                        <?php endforeach ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
<?php endif ?>
