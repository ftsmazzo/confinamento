<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
<?php $this->end() ?>

<form id="form-parametro-trato" class="form-validate" action="<?=$url_action?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf?>">

    <div class="card crud-form-card">
        <div class="card-body">
            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Projeção de consumo</h5>
                    <small>Usados pelo motor: peso projetado = peso de entrada + (GMD × dias); consumo MS = peso × %PV.</small>
                </div>
                <div class="row">
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="trato_gmd" class="form-label">GMD (kg/dia)</label>
                            <input type="text" class="form-control input-money" id="trato_gmd" name="trato_gmd" placeholder="1,50" autocomplete="off" value="<?=number_format((float) $params["gmd"], 2, ",", ".")?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="trato_pct_peso_vivo" class="form-label">% do peso vivo (consumo MS)</label>
                            <input type="text" class="form-control input-money" id="trato_pct_peso_vivo" name="trato_pct_peso_vivo" placeholder="2,25" autocomplete="off" value="<?=number_format((float) $params["pct_peso_vivo"], 2, ",", ".")?>">
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Rateio dos 4 turnos (%)</h5>
                    <small>A soma deve fechar em 100%. Padrão da planilha: 30 / 20 / 20 / 30.</small>
                </div>
                <div class="row">
                    <?php
                        $turnoFields = [
                            ["field" => "trato_turno_1_pct", "code" => "MANHA"],
                            ["field" => "trato_turno_2_pct", "code" => "MEIO"],
                            ["field" => "trato_turno_3_pct", "code" => "TARDE"],
                            ["field" => "trato_turno_4_pct", "code" => "NOITE"],
                        ];
                        foreach ($turnoFields as $idx => $tf) :
                            $pct = (float) ($params["turnos"][$tf["code"]] ?? 0);
                            $label = $turnosLabel[$tf["code"]] ?? ("Turno " . ($idx + 1));
                    ?>
                        <div class="col-6 col-md-3">
                            <div class="mb-3">
                                <label for="<?=$tf["field"]?>" class="form-label"><?=$label?></label>
                                <input type="text" class="form-control input-decimal" id="<?=$tf["field"]?>" name="<?=$tf["field"]?>" placeholder="0,0" autocomplete="off" value="<?=number_format($pct, 1, ",", ".")?>">
                            </div>
                        </div>
                    <?php endforeach ?>
                </div>
            </div>

            <div class="crud-subsection">
                <div class="crud-section-copy mb-3">
                    <h5>Alerta de permanência</h5>
                    <small>Exibido na listagem de lotes (atenção ~100 dias; risco ~110).</small>
                </div>
                <div class="row">
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="trato_alerta_dias" class="form-label">Atenção a partir de (dias)</label>
                            <input type="number" class="form-control" id="trato_alerta_dias" name="trato_alerta_dias" min="1" step="1" value="<?=(int) $params["alerta_dias"]?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="trato_alerta_risco_dias" class="form-label">Risco a partir de (dias)</label>
                            <input type="number" class="form-control" id="trato_alerta_risco_dias" name="trato_alerta_risco_dias" min="1" step="1" value="<?=(int) $params["alerta_risco_dias"]?>">
                        </div>
                    </div>
                </div>
            </div>

            <div class="mt-3 pt-4 border-top">
                <div class="d-grid gap-2 d-sm-block">
                    <button class="btn btn-primary" type="submit">Salvar</button>
                    <a href="<?=$router->route("admin.nutricao.programacao.trato.index")?>" class="btn btn-white"><i class="uil uil-left"></i> Voltar</a>
                </div>
            </div>
        </div>
    </div>
</form>
