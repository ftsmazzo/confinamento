<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
<?php $this->end() ?>

<form id="form-nutricao-ingrediente" class="form-validate" action="<?=$url_action?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf?>">
    <?php if (!empty($ingrediente->id)) : ?>
        <input type="hidden" name="id" value="<?=$ingrediente->hash()?>">
    <?php endif ?>

    <div class="card crud-form-card">
        <div class="card-body">
            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Identificação</h5>
                    <small>Dados básicos do ingrediente.</small>
                </div>

                <div class="row">
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="nome" class="form-label">Nome</label>
                            <input type="text" class="form-control" id="nome" name="nome" placeholder="Ex: Milho, Farelo de Soja, Silagem" autocomplete="off" value="<?=$ingrediente->nome ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="id_grupo_ingrediente" class="form-label">Grupo</label>
                            <select name="id_grupo_ingrediente" id="id_grupo_ingrediente" class="form-select select2" data-search="true">
                                <option value="">Selecione</option>
                                <?php foreach ($grupos as $g) : ?>
                                    <option value="<?=$g->id?>" <?=isset($ingrediente->id_grupo_ingrediente) && (int) $ingrediente->id_grupo_ingrediente === (int) $g->id ? "selected" : ""?>><?=$g->descricao?></option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Estoque</h5>
                    <small>Unidade de medida e controle de saldo.</small>
                </div>

                <div class="row">
                    <div class="col-12 col-md-3">
                        <div class="mb-3">
                            <label for="unidade_medida" class="form-label">Unidade</label>
                            <input type="text" class="form-control" id="unidade_medida" name="unidade_medida" placeholder="KG" autocomplete="off" maxlength="10" value="<?=$ingrediente->unidade_medida ?? "KG"?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="estoque_atual" class="form-label">Estoque Atual</label>
                            <input type="text" class="form-control input-decimal" id="estoque_atual" name="estoque_atual" placeholder="0,00" autocomplete="off" value="<?=isset($ingrediente->estoque_atual) ? number_format((float) $ingrediente->estoque_atual, 2, ",", ".") : ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-5">
                        <div class="mb-3">
                            <label for="estoque_minimo" class="form-label">Estoque Mínimo</label>
                            <input type="text" class="form-control input-decimal" id="estoque_minimo" name="estoque_minimo" placeholder="Opcional, para alertas" autocomplete="off" value="<?=isset($ingrediente->estoque_minimo) ? number_format((float) $ingrediente->estoque_minimo, 2, ",", ".") : ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="custo_unitario" class="form-label">Custo Unitário (R$)</label>
                            <input type="text" class="form-control input-money" id="custo_unitario" name="custo_unitario" placeholder="Opcional, usado em relatórios" autocomplete="off" value="<?=isset($ingrediente->custo_unitario) ? number_format((float) $ingrediente->custo_unitario, 2, ",", ".") : ""?>">
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection">
                <div class="crud-section-copy mb-3">
                    <h5>Situação</h5>
                    <small>Defina se o ingrediente está ativo.</small>
                </div>

                <div class="row">
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="ativo" class="form-label">Ativo</label>
                            <select name="ativo" id="ativo" class="form-select" data-search="false">
                                <option value="1" <?=(!isset($ingrediente->ativo) || (int) $ingrediente->ativo === 1) ? "selected" : ""?>>Sim</option>
                                <option value="0" <?=isset($ingrediente->ativo) && (int) $ingrediente->ativo === 0 ? "selected" : ""?>>Não</option>
                            </select>
                        </div>
                    </div>
                </div>
            </div>

            <div class="mt-3 pt-4 border-top">
                <div class="d-grid gap-2 d-sm-block">
                    <button class="btn btn-primary" type="submit">Salvar</button>
                    <a href="<?=$router->route("admin.nutricao.ingrediente.index")?>" class="btn btn-white"><i class="uil uil-left"></i> Voltar</a>
                </div>
            </div>
        </div>
    </div>
</form>
