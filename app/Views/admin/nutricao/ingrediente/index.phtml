<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-buttons") ?>
    <?php if (!empty($permissao["inserir"])) : ?>
        <a class="btn btn-primary" href="<?=$router->route("admin.nutricao.ingrediente.novo")?>"><i class="fa fa-plus fa-fw"></i> Novo Ingrediente</a>
    <?php endif ?>
<?php $this->end() ?>

<div class="card crud-table-card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover table-sm table-datatable table-datatable-alt crud-table">
                <thead>
                    <tr>
                        <th>Nome</th>
                        <th>Grupo</th>
                        <th>Unidade</th>
                        <th>Estoque Atual</th>
                        <th>Custo Unit.</th>
                        <th>Ativo</th>
                        <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                            <th class="text-end actions-column"></th>
                        <?php endif ?>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($dados as $d) : ?>
                        <tr>
                            <td>
                                <?php if ($permissao["editar"]) : ?>
                                    <a class="edit" href="<?=$router->route("admin.nutricao.ingrediente.editar", ["id" => $d->hash])?>"><?=$d->nome?></a>
                                <?php else : ?>
                                    <span><?=$d->nome?></span>
                                <?php endif ?>
                            </td>
                            <td><?=$d->grupo_descricao ?: "-"?></td>
                            <td><?=$d->unidade_medida?></td>
                            <td>
                                <?=number_format((float) $d->estoque_atual, 2, ",", ".")?>
                                <?php if (!empty($d->estoque_baixo)) : ?>
                                    <span class="badge filled-outlined bg-danger" data-tooltip="true" title="Abaixo do estoque mínimo">Baixo</span>
                                <?php endif ?>
                            </td>
                            <td><?=$d->custo_unitario !== null ? "R$ " . number_format((float) $d->custo_unitario, 2, ",", ".") : "-"?></td>
                            <td><?=!empty($d->ativo) ? '<span class="badge filled-outlined bg-success">Sim</span>' : '<span class="badge filled-outlined bg-secondary">Não</span>'?></td>
                            <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                                <td class="text-end actions-column">
                                    <div class="table-actions">
                                        <?php if ($permissao["editar"]) : ?>
                                            <a class="table-action-btn" data-tooltip="true" title="Editar" href="<?=$router->route("admin.nutricao.ingrediente.editar", ["id" => $d->hash])?>"><i class="uil-edit-alt"></i></a>
                                        <?php endif ?>
                                        <?php if ($permissao["excluir"]) : ?>
                                            <button type="button" class="table-action-btn table-action-danger" data-tooltip="true" title="Excluir ingrediente" onclick="Delete('nutricao/ingredientes/delete', '<?=$d->hash?>')"><i class="uil uil-trash"></i></button>
                                        <?php endif ?>
                                    </div>
                                </td>
                            <?php endif ?>
                        </tr>
                    <?php endforeach ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
