<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
<?php $this->end() ?>

<form id="form-nutricao-fornecimento-trato" class="form-validate" action="<?=$url_action?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf?>">
    <?php if (!empty($fornecimento->id)) : ?>
        <input type="hidden" name="id" value="<?=$fornecimento->hash()?>">
    <?php endif ?>

    <div class="card crud-form-card">
        <div class="card-body">
            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Fornecimento</h5>
                    <small>Registro do trato efetivamente entregue ao lote, opcionalmente vinculado a uma programação.</small>
                </div>

                <div class="row">
                    <?php if ($id_lote) : ?>
                        <input type="hidden" name="id_lote" value="<?=$id_lote?>">
                        <?php $loteAtual = null; foreach ($lotes as $l) { if ((int) $l->id === (int) $id_lote) { $loteAtual = $l; break; } } ?>
                        <div class="col-12">
                            <div class="alert alert-info mb-0">Fornecendo trato ao lote: <strong><?=$loteAtual->nome ?? "#$id_lote"?></strong></div>
                        </div>
                    <?php else : ?>
                        <div class="col-12 col-md-6">
                            <div class="mb-3">
                                <label for="id_lote" class="form-label">Lote</label>
                                <select name="id_lote" id="id_lote" class="form-select select2" data-search="true">
                                    <option value="">Selecione</option>
                                    <?php foreach ($lotes as $l) : ?>
                                        <option value="<?=$l->id?>" data-id-curral="<?=!empty($l->id_curral) ? (int) $l->id_curral : ""?>" <?=isset($fornecimento->id_lote) && (int) $fornecimento->id_lote === (int) $l->id ? "selected" : ""?>><?=$l->nome?> (<?=$l->codigo?>)</option>
                                    <?php endforeach ?>
                                </select>
                            </div>
                        </div>
                    <?php endif ?>
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="id_programacao_trato" class="form-label">Programação (opcional)</label>
                            <select name="id_programacao_trato" id="id_programacao_trato" class="form-select select2" data-search="true">
                                <option value="">Nenhuma / lançamento direto</option>
                                <?php foreach ($programacoes as $p) : ?>
                                    <option value="<?=$p->id?>" <?=isset($fornecimento->id_programacao_trato) && (int) $fornecimento->id_programacao_trato === (int) $p->id ? "selected" : ""?>><?=datebr($p->data_programacao)?> <?=$p->turno ? "({$p->turno})" : ""?></option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-3">
                        <div class="mb-3">
                            <label for="data_fornecimento" class="form-label">Data</label>
                            <input type="date" class="form-control" id="data_fornecimento" name="data_fornecimento" autocomplete="off" value="<?=!empty($fornecimento->data_fornecimento) ? dateus($fornecimento->data_fornecimento) : ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-3">
                        <div class="mb-3">
                            <label for="hora_fornecimento" class="form-label">Hora</label>
                            <input type="time" class="form-control" id="hora_fornecimento" name="hora_fornecimento" autocomplete="off" value="<?=$fornecimento->hora_fornecimento ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="id_curral" class="form-label">Curral</label>
                            <select name="id_curral" id="id_curral" class="form-select select2" data-search="true">
                                <option value="">Selecione</option>
                                <?php foreach ($currais as $c) : ?>
                                    <option value="<?=$c->id?>" data-id-lote="<?=!empty($c->id_lote_ativo) ? (int) $c->id_lote_ativo : ""?>" <?=isset($fornecimento->id_curral) && (int) $fornecimento->id_curral === (int) $c->id ? "selected" : ""?>><?=$c->nome?><?=!empty($c->linha) ? " · " . $c->linha : ""?></option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="id_formula_racao" class="form-label">Fórmula de Ração</label>
                            <select name="id_formula_racao" id="id_formula_racao" class="form-select select2" data-search="true">
                                <option value="">Selecione</option>
                                <?php foreach ($formulas as $formula) : ?>
                                    <option value="<?=$formula->id?>" <?=isset($fornecimento->id_formula_racao) && (int) $fornecimento->id_formula_racao === (int) $formula->id ? "selected" : ""?>><?=$formula->nome?></option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="quantidade_fornecida" class="form-label">Qtd. Fornecida</label>
                            <input type="text" class="form-control input-decimal" id="quantidade_fornecida" name="quantidade_fornecida" placeholder="0,00" autocomplete="off" value="<?=isset($fornecimento->quantidade_fornecida) ? number_format((float) $fornecimento->quantidade_fornecida, 2, ",", ".") : ""?>">
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection">
                <div class="crud-section-copy mb-3">
                    <h5>Observações</h5>
                    <small>Informações livres para apoio operacional.</small>
                </div>

                <div class="row">
                    <div class="col-12">
                        <div class="mb-3">
                            <textarea class="form-control" id="observacao" name="observacao" rows="3" placeholder="Observação"><?=$fornecimento->observacao ?? ""?></textarea>
                        </div>
                    </div>
                </div>
            </div>

            <div class="mt-3 pt-4 border-top">
                <div class="d-grid gap-2 d-sm-block">
                    <button class="btn btn-primary" type="submit">Salvar</button>
                    <a href="<?=$url_voltar?>" class="btn btn-white"><i class="uil uil-left"></i> Voltar</a>
                </div>
            </div>
        </div>
    </div>
</form>

<script src="<?=asset("admin/js/curral-lote-link.js")?>"></script>
