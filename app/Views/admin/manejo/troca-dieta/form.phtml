<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
<?php $this->end() ?>

<form id="form-manejo-troca-dieta" class="form-validate" action="<?=$url_action?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf?>">
    <?php if (!empty($troca->id)) : ?>
        <input type="hidden" name="id" value="<?=$troca->hash()?>">
    <?php endif ?>

    <div class="card crud-form-card">
        <div class="card-body">
            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Lote e Nova Fórmula</h5>
                    <small>Qual lote muda de dieta e para qual fórmula de ração.</small>
                </div>

                <div class="row">
                    <?php if ($id_lote) : ?>
                        <input type="hidden" name="id_lote" value="<?=$id_lote?>">
                        <?php $loteAtual = null; foreach ($lotes as $l) { if ((int) $l->id === (int) $id_lote) { $loteAtual = $l; break; } } ?>
                        <div class="col-12">
                            <div class="alert alert-info mb-0">Trocando dieta do lote: <strong><?=$loteAtual->nome ?? "#$id_lote"?></strong></div>
                        </div>
                    <?php else : ?>
                        <div class="col-12 col-md-6">
                            <div class="mb-3">
                                <label for="id_lote" class="form-label">Lote</label>
                                <select name="id_lote" id="id_lote" class="form-select select2" data-search="true">
                                    <option value="">Selecione</option>
                                    <?php foreach ($lotes as $l) : ?>
                                        <option value="<?=$l->id?>" <?=isset($troca->id_lote) && (int) $troca->id_lote === (int) $l->id ? "selected" : ""?>><?=$l->nome?> (<?=$l->codigo?>)</option>
                                    <?php endforeach ?>
                                </select>
                            </div>
                        </div>
                    <?php endif ?>
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="id_formula_racao" class="form-label">Fórmula de Ração</label>
                            <select name="id_formula_racao" id="id_formula_racao" class="form-select select2" data-search="true">
                                <option value="">Selecione</option>
                                <?php foreach ($formulas as $formula) : ?>
                                    <option value="<?=$formula->id?>" <?=isset($troca->id_formula_racao) && (int) $troca->id_formula_racao === (int) $formula->id ? "selected" : ""?>><?=$formula->nome?></option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="data_troca" class="form-label">Data da Troca</label>
                            <input type="date" class="form-control" id="data_troca" name="data_troca" autocomplete="off" value="<?=!empty($troca->data_troca) ? dateus($troca->data_troca) : ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-8">
                        <div class="mb-3">
                            <label for="motivo" class="form-label">Motivo</label>
                            <input type="text" class="form-control" id="motivo" name="motivo" placeholder="Ex: mudança de fase, leitura de cocho" autocomplete="off" value="<?=$troca->motivo ?? ""?>">
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection">
                <div class="crud-section-copy mb-3">
                    <h5>Observações</h5>
                    <small>Informações livres para apoio operacional.</small>
                </div>

                <div class="row">
                    <div class="col-12">
                        <div class="mb-3">
                            <textarea class="form-control" id="observacao" name="observacao" rows="4" placeholder="Observação"><?=$troca->observacao ?? ""?></textarea>
                        </div>
                    </div>
                </div>
            </div>

            <div class="mt-3 pt-4 border-top">
                <div class="d-grid gap-2 d-sm-block">
                    <button class="btn btn-primary" type="submit">Salvar</button>
                    <a href="<?=$url_voltar?>" class="btn btn-white"><i class="uil uil-left"></i> Voltar</a>
                </div>
            </div>
        </div>
    </div>
</form>
