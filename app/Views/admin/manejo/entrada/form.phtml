<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
<?php $this->end() ?>

<form id="form-manejo-entrada" class="form-validate" action="<?=$url_action?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf?>">
    <?php if (!empty($entrada->id)) : ?>
        <input type="hidden" name="id" value="<?=$entrada->hash()?>">
    <?php endif ?>

    <div class="card crud-form-card">
        <div class="card-body">
            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Lote e Origem</h5>
                    <small>De onde vieram os animais e para qual lote.</small>
                </div>

                <div class="row">
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="id_lote" class="form-label">Lote</label>
                            <select name="id_lote" id="id_lote" class="form-select select2" data-search="true">
                                <option value="">Selecione</option>
                                <?php foreach (($lotes ?? []) as $lote) : ?>
                                    <option value="<?=$lote->id?>" <?=isset($entrada->id_lote) && (int) $entrada->id_lote === (int) $lote->id ? "selected" : ""?>><?=$lote->nome?> (<?=$lote->codigo?>)</option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="id_fornecedor" class="form-label">Fornecedor</label>
                            <select name="id_fornecedor" id="id_fornecedor" class="form-select select2" data-search="true">
                                <option value="">Selecione</option>
                                <?php foreach (($fornecedores ?? []) as $fornecedor) : ?>
                                    <option value="<?=$fornecedor->id?>" <?=isset($entrada->id_fornecedor) && (int) $entrada->id_fornecedor === (int) $fornecedor->id ? "selected" : ""?>><?=$fornecedor->razao?></option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="id_tipo_entrada" class="form-label">Tipo de Entrada</label>
                            <select name="id_tipo_entrada" id="id_tipo_entrada" class="form-select" data-search="false">
                                <option value="">Selecione</option>
                                <?php foreach (($tiposEntrada ?? []) as $tipo) : ?>
                                    <option value="<?=$tipo->id?>" <?=isset($entrada->id_tipo_entrada) && (int) $entrada->id_tipo_entrada === (int) $tipo->id ? "selected" : ""?>><?=$tipo->descricao?></option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="id_curral_destino" class="form-label">Curral de Destino</label>
                            <select name="id_curral_destino" id="id_curral_destino" class="form-select select2" data-search="true">
                                <option value="">Selecione</option>
                                <?php foreach (($currais ?? []) as $curral) : ?>
                                    <option value="<?=$curral->id?>" <?=isset($entrada->id_curral_destino) && (int) $entrada->id_curral_destino === (int) $curral->id ? "selected" : ""?>><?=$curral->nome?> (<?=$curral->codigo?>)</option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="data_entrada" class="form-label">Data de Entrada</label>
                            <input type="date" class="form-control" id="data_entrada" name="data_entrada" autocomplete="off" value="<?=!empty($entrada->data_entrada) ? dateus($entrada->data_entrada) : ""?>">
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Documentação e Valores</h5>
                    <small>Nota fiscal, GTA e valor de compra.</small>
                </div>

                <div class="row">
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="tipo_documento" class="form-label">Tipo de Documento</label>
                            <select name="tipo_documento" id="tipo_documento" class="form-select" data-search="false">
                                <option value="">Selecione</option>
                                <option value="NF" <?=isset($entrada->tipo_documento) && $entrada->tipo_documento === "NF" ? "selected" : ""?>>Nota Fiscal</option>
                                <option value="GTA" <?=isset($entrada->tipo_documento) && $entrada->tipo_documento === "GTA" ? "selected" : ""?>>GTA</option>
                                <option value="OUTRO" <?=isset($entrada->tipo_documento) && $entrada->tipo_documento === "OUTRO" ? "selected" : ""?>>Outro</option>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="documento" class="form-label">Número do Documento</label>
                            <input type="text" class="form-control" id="documento" name="documento" placeholder="Número da NF ou GTA" autocomplete="off" value="<?=$entrada->documento ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="valor_total" class="form-label">Valor Total (R$)</label>
                            <input type="text" class="form-control input-money" id="valor_total" name="valor_total" placeholder="Valor pago" autocomplete="off" value="<?=$entrada->valor_total ?? ""?>">
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection">
                <div class="crud-section-copy mb-3">
                    <h5>Observações</h5>
                    <small>Informações livres para apoio operacional.</small>
                </div>

                <div class="row">
                    <div class="col-12">
                        <div class="mb-3">
                            <textarea class="form-control" id="observacao" name="observacao" rows="4" placeholder="Observação"><?=$entrada->observacao ?? ""?></textarea>
                        </div>
                    </div>
                </div>
            </div>

            <div class="mt-3 pt-4 border-top">
                <div class="d-grid gap-2 d-sm-block">
                    <button class="btn btn-primary" type="submit">Salvar</button>
                    <a href="<?=$router->route("admin.manejo.entrada.index")?>" class="btn btn-white"><i class="uil uil-left"></i> Voltar</a>
                </div>
            </div>
        </div>
    </div>
</form>
