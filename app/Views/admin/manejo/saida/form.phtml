<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
<?php $this->end() ?>

<form id="form-manejo-saida" class="form-validate" action="<?=$url_action?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf?>">
    <?php if (!empty($saida->id)) : ?>
        <input type="hidden" name="id" value="<?=$saida->hash()?>">
    <?php endif ?>

    <div class="card crud-form-card">
        <div class="card-body">
            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Lote e Destino</h5>
                    <small>Qual lote saiu e para onde.</small>
                </div>

                <div class="row">
                    <?php if ($id_lote) : ?>
                        <input type="hidden" name="id_lote" value="<?=$id_lote?>">
                        <?php $loteAtual = null; foreach ($lotes as $l) { if ((int) $l->id === (int) $id_lote) { $loteAtual = $l; break; } } ?>
                        <div class="col-12">
                            <div class="alert alert-info mb-0">Registrando saída do lote: <strong><?=$loteAtual->nome ?? "#$id_lote"?></strong></div>
                        </div>
                    <?php else : ?>
                        <div class="col-12 col-md-6">
                            <div class="mb-3">
                                <label for="id_lote" class="form-label">Lote</label>
                                <select name="id_lote" id="id_lote" class="form-select select2" data-search="true">
                                    <option value="">Selecione</option>
                                    <?php foreach ($lotes as $l) : ?>
                                        <option value="<?=$l->id?>" <?=isset($saida->id_lote) && (int) $saida->id_lote === (int) $l->id ? "selected" : ""?>><?=$l->nome?> (<?=$l->codigo?>)</option>
                                    <?php endforeach ?>
                                </select>
                            </div>
                        </div>
                    <?php endif ?>
                    <div class="col-12 col-md-3">
                        <div class="mb-3">
                            <label for="id_tipo_saida" class="form-label">Tipo de Saída</label>
                            <select name="id_tipo_saida" id="id_tipo_saida" class="form-select" data-search="false">
                                <option value="">Selecione</option>
                                <?php foreach ($tiposSaida as $tipo) : ?>
                                    <option value="<?=$tipo->id?>" <?=isset($saida->id_tipo_saida) && (int) $saida->id_tipo_saida === (int) $tipo->id ? "selected" : ""?>><?=$tipo->descricao?></option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-3">
                        <div class="mb-3">
                            <label for="data_saida" class="form-label">Data</label>
                            <input type="date" class="form-control" id="data_saida" name="data_saida" autocomplete="off" value="<?=!empty($saida->data_saida) ? dateus($saida->data_saida) : ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="id_cliente" class="form-label">Cliente / Comprador <small class="text-muted">(opcional)</small></label>
                            <select name="id_cliente" id="id_cliente" class="form-select select2" data-search="true">
                                <option value="">Nenhum</option>
                                <?php foreach ($clientes as $cliente) : ?>
                                    <option value="<?=$cliente->id?>" <?=isset($saida->id_cliente) && (int) $saida->id_cliente === (int) $cliente->id ? "selected" : ""?>><?=$cliente->razao?></option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Quantidade, Peso e Valor</h5>
                    <small>Dados da baixa dos animais.</small>
                </div>

                <div class="row">
                    <div class="col-12 col-md-3">
                        <div class="mb-3">
                            <label for="quantidade" class="form-label">Quantidade</label>
                            <input type="number" min="0" class="form-control" id="quantidade" name="quantidade" placeholder="Nº de cabeças" autocomplete="off" value="<?=$saida->quantidade ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-3">
                        <div class="mb-3">
                            <label for="peso_medio" class="form-label">Peso Médio (kg)</label>
                            <input type="text" class="form-control input-peso" id="peso_medio" name="peso_medio" placeholder="Peso médio" autocomplete="off" value="<?=$saida->peso_medio ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-3">
                        <div class="mb-3">
                            <label for="peso_total" class="form-label">Peso Total (kg)</label>
                            <input type="text" class="form-control input-peso" id="peso_total" name="peso_total" placeholder="Peso total" autocomplete="off" value="<?=$saida->peso_total ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-3">
                        <div class="mb-3">
                            <label for="valor_total" class="form-label">Valor Total (R$)</label>
                            <input type="text" class="form-control input-money" id="valor_total" name="valor_total" placeholder="Valor recebido" autocomplete="off" value="<?=$saida->valor_total ?? ""?>">
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Resultado</h5>
                    <small>Observações sobre o resultado financeiro/zootécnico da saída.</small>
                </div>

                <div class="row">
                    <div class="col-12">
                        <div class="mb-3">
                            <textarea class="form-control" id="resultado" name="resultado" rows="3" placeholder="Resultado"><?=$saida->resultado ?? ""?></textarea>
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection">
                <div class="crud-section-copy mb-3">
                    <h5>Observações</h5>
                    <small>Informações livres para apoio operacional.</small>
                </div>

                <div class="row">
                    <div class="col-12">
                        <div class="mb-3">
                            <textarea class="form-control" id="observacao" name="observacao" rows="4" placeholder="Observação"><?=$saida->observacao ?? ""?></textarea>
                        </div>
                    </div>
                </div>
            </div>

            <div class="mt-3 pt-4 border-top">
                <div class="d-grid gap-2 d-sm-block">
                    <button class="btn btn-primary" type="submit">Salvar</button>
                    <a href="<?=$url_voltar?>" class="btn btn-white"><i class="uil uil-left"></i> Voltar</a>
                </div>
            </div>
        </div>
    </div>
</form>
