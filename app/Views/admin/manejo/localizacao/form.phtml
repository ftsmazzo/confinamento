<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
<?php $this->end() ?>

<form id="form-manejo-localizacao" class="form-validate" action="<?=$url_action?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf?>">
    <?php if (!empty($localizacao->id)) : ?>
        <input type="hidden" name="id" value="<?=$localizacao->hash()?>">
    <?php endif ?>

    <div class="card crud-form-card">
        <div class="card-body">
            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Lote</h5>
                    <small>Qual lote está sendo alocado ou transferido.</small>
                </div>

                <div class="row">
                    <?php if ($id_lote) : ?>
                        <input type="hidden" name="id_lote" value="<?=$id_lote?>">
                        <div class="col-12">
                            <div class="alert alert-info mb-0">Movimentando o lote: <strong><?=$lote->nome ?? "#$id_lote"?></strong></div>
                        </div>
                    <?php else : ?>
                        <div class="col-12 col-md-6">
                            <div class="mb-3">
                                <label for="id_lote" class="form-label">Lote</label>
                                <select name="id_lote" id="id_lote" class="form-select select2" data-search="true">
                                    <option value="">Selecione</option>
                                    <?php foreach (($lotes ?? []) as $l) : ?>
                                        <option value="<?=$l->id?>" <?=isset($localizacao->id_lote) && (int) $localizacao->id_lote === (int) $l->id ? "selected" : ""?>><?=$l->nome?> (<?=$l->codigo?>)</option>
                                    <?php endforeach ?>
                                </select>
                            </div>
                        </div>
                    <?php endif ?>
                </div>
            </div>

            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Origem (opcional)</h5>
                    <small>Deixe em branco se for a primeira alocação do lote.</small>
                </div>

                <div class="row">
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="id_curral_origem" class="form-label">Curral de Origem</label>
                            <select name="id_curral_origem" id="id_curral_origem" class="form-select select2" data-search="true">
                                <option value="">Nenhum</option>
                                <?php foreach (($currais ?? []) as $curral) : ?>
                                    <option value="<?=$curral->id?>" <?=isset($localizacao->id_curral_origem) && (int) $localizacao->id_curral_origem === (int) $curral->id ? "selected" : ""?>><?=$curral->nome?> (<?=$curral->codigo?>)</option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="id_piquete_origem" class="form-label">Piquete de Origem</label>
                            <select name="id_piquete_origem" id="id_piquete_origem" class="form-select select2" data-search="true">
                                <option value="">Nenhum</option>
                                <?php foreach (($piquetes ?? []) as $piquete) : ?>
                                    <option value="<?=$piquete->id?>" <?=isset($localizacao->id_piquete_origem) && (int) $localizacao->id_piquete_origem === (int) $piquete->id ? "selected" : ""?>><?=$piquete->nome?> (<?=$piquete->codigo?>)</option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Destino</h5>
                    <small>Informe o curral OU o piquete de destino.</small>
                </div>

                <div class="row">
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="id_curral_destino" class="form-label">Curral de Destino</label>
                            <select name="id_curral_destino" id="id_curral_destino" class="form-select select2" data-search="true">
                                <option value="">Nenhum</option>
                                <?php foreach (($currais ?? []) as $curral) : ?>
                                    <option value="<?=$curral->id?>" <?=isset($localizacao->id_curral_destino) && (int) $localizacao->id_curral_destino === (int) $curral->id ? "selected" : ""?>><?=$curral->nome?> (<?=$curral->codigo?>)</option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="id_piquete_destino" class="form-label">Piquete de Destino</label>
                            <select name="id_piquete_destino" id="id_piquete_destino" class="form-select select2" data-search="true">
                                <option value="">Nenhum</option>
                                <?php foreach (($piquetes ?? []) as $piquete) : ?>
                                    <option value="<?=$piquete->id?>" <?=isset($localizacao->id_piquete_destino) && (int) $localizacao->id_piquete_destino === (int) $piquete->id ? "selected" : ""?>><?=$piquete->nome?> (<?=$piquete->codigo?>)</option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="data_movimentacao" class="form-label">Data</label>
                            <input type="date" class="form-control" id="data_movimentacao" name="data_movimentacao" autocomplete="off" value="<?=!empty($localizacao->data_movimentacao) ? dateus($localizacao->data_movimentacao) : ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="quantidade" class="form-label">Quantidade Movida</label>
                            <input type="number" min="0" class="form-control" id="quantidade" name="quantidade" placeholder="Vazio = lote inteiro" autocomplete="off" value="<?=$localizacao->quantidade ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="motivo" class="form-label">Motivo</label>
                            <input type="text" class="form-control" id="motivo" name="motivo" placeholder="Motivo da movimentação" autocomplete="off" value="<?=$localizacao->motivo ?? ""?>">
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection">
                <div class="crud-section-copy mb-3">
                    <h5>Observações</h5>
                    <small>Informações livres para apoio operacional.</small>
                </div>

                <div class="row">
                    <div class="col-12">
                        <div class="mb-3">
                            <textarea class="form-control" id="observacao" name="observacao" rows="4" placeholder="Observação"><?=$localizacao->observacao ?? ""?></textarea>
                        </div>
                    </div>
                </div>
            </div>

            <div class="mt-3 pt-4 border-top">
                <div class="d-grid gap-2 d-sm-block">
                    <button class="btn btn-primary" type="submit">Salvar</button>
                    <a href="<?=$url_voltar?>" class="btn btn-white"><i class="uil uil-left"></i> Voltar</a>
                </div>
            </div>
        </div>
    </div>
</form>
