<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
<?php $this->end() ?>

<form id="form-lembrete" class="form-validate" action="<?=$url_action?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf?>">
    <?php if (!empty($lembrete->id)) : ?>
        <input type="hidden" name="id" value="<?=$lembrete->hash()?>">
    <?php endif ?>

    <div class="card crud-form-card">
        <div class="card-body">
            <div class="crud-subsection">
                <div class="crud-section-copy mb-3">
                    <h5>Lembrete</h5>
                    <small>Se pertinente, vincule a um lote ou animal (nunca os dois). O lembrete aparece no Calendário na data informada.</small>
                </div>

                <div class="row">
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="id_lote" class="form-label">Lote (opcional)</label>
                            <select name="id_lote" id="id_lote" class="form-select select2" data-search="true">
                                <option value="">Nenhum</option>
                                <?php foreach ($lotes as $l) : ?>
                                    <option value="<?=$l->id?>" <?=isset($lembrete->id_lote) && (int) $lembrete->id_lote === (int) $l->id ? "selected" : ""?>><?=$l->nome?> (<?=$l->codigo?>)</option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="id_animal" class="form-label">Animal (opcional)</label>
                            <select name="id_animal" id="id_animal" class="form-select select2" data-search="true">
                                <option value="">Nenhum</option>
                                <?php foreach ($animais as $a) : ?>
                                    <option value="<?=$a->id?>" <?=isset($lembrete->id_animal) && (int) $lembrete->id_animal === (int) $a->id ? "selected" : ""?>><?=$a->identificacao?></option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="data_lembrete" class="form-label">Data</label>
                            <input type="date" class="form-control" id="data_lembrete" name="data_lembrete" autocomplete="off" value="<?=$data_lembrete ? dateus($data_lembrete) : ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-8">
                        <div class="mb-3">
                            <label for="titulo" class="form-label">Título</label>
                            <input type="text" class="form-control" id="titulo" name="titulo" placeholder="Ex: Revacinação, retirar lote da carência, etc." autocomplete="off" value="<?=$lembrete->titulo ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12">
                        <div class="mb-3">
                            <label for="descricao" class="form-label">Descrição</label>
                            <textarea class="form-control" id="descricao" name="descricao" rows="3" placeholder="Detalhes do lembrete"><?=$lembrete->descricao ?? ""?></textarea>
                        </div>
                    </div>
                    <?php if (!empty($lembrete->id)) : ?>
                        <div class="col-12">
                            <div class="form-check">
                                <input type="checkbox" class="form-check-input" id="concluido" name="concluido" value="1" <?=!empty($lembrete->concluido) ? "checked" : ""?>>
                                <label class="form-check-label" for="concluido">Concluído</label>
                            </div>
                        </div>
                    <?php endif ?>
                </div>
            </div>

            <div class="mt-3 pt-4 border-top">
                <div class="d-grid gap-2 d-sm-block">
                    <button class="btn btn-primary" type="submit">Salvar</button>
                    <a href="<?=$router->route("admin.calendario.index")?>" class="btn btn-white"><i class="uil uil-left"></i> Voltar</a>
                </div>
            </div>
        </div>
    </div>
</form>
