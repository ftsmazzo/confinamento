<?php $this->layout("app/template/base"); ?>

<div class="card mb-4">
    <div class="card-body">
        <form method="GET" action="<?=$router->route("admin.financeiro.relatorio.fluxo.caixa")?>" class="row g-2 align-items-end">
            <div class="col-6 col-md-4">
                <label class="form-label small text-muted mb-1">Iniciar em</label>
                <input type="date" name="data_inicial" class="form-control form-control-sm" value="<?=$dataInicial?>">
            </div>
            <div class="col-6 col-md-4">
                <label class="form-label small text-muted mb-1">Meses</label>
                <select name="meses" class="form-select form-select-sm" onchange="this.form.submit()">
                    <?php foreach ([3, 6, 12, 18, 24] as $m) : ?>
                        <option value="<?=$m?>" <?=$meses === $m ? "selected" : ""?>><?=$m?> meses</option>
                    <?php endforeach ?>
                </select>
            </div>
            <div class="col-12 col-md-4">
                <button type="submit" class="btn btn-sm btn-primary w-100">Filtrar</button>
            </div>
        </form>
    </div>
</div>

<div class="card crud-table-card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover table-sm datatable-no-paging">
                <thead>
                    <tr>
                        <th>Mês</th>
                        <th>Entradas</th>
                        <th>Saídas</th>
                        <th>Saldo do Mês</th>
                        <th>Saldo Acumulado</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($mesesData as $mes => $vals) : ?>
                        <tr>
                            <td class="text-nowrap"><?=strtoupper(date("M/Y", strtotime($mes . "-01")))?></td>
                            <td class="text-nowrap text-success">R$ <?=number_format($vals["entradas"], 2, ',', '.')?></td>
                            <td class="text-nowrap text-danger">R$ <?=number_format($vals["saidas"], 2, ',', '.')?></td>
                            <td class="text-nowrap <?=$vals["saldo"] >= 0 ? "text-success" : "text-danger"?>">
                                R$ <?=number_format($vals["saldo"], 2, ',', '.')?>
                            </td>
                            <td class="text-nowrap <?=$vals["acumulado"] >= 0 ? "text-success" : "text-danger"?>">
                                R$ <?=number_format($vals["acumulado"], 2, ',', '.')?>
                            </td>
                        </tr>
                    <?php endforeach ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
