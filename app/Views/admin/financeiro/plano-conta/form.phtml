<?php $this->layout("app/template/base"); ?>

<form id="form-financeiro-plano-conta" class="form-validate" action="<?=$url_action?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf?>">
    <?php if (!empty($conta->id)) : ?>
        <input type="hidden" name="id" value="<?=$conta->hash()?>">
    <?php endif ?>

    <div class="card crud-form-card">
        <div class="card-body">
            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Identificação</h5>
                    <small>Código e nome da conta contábil.</small>
                </div>
                <div class="row">
                    <div class="col-12 col-md-3">
                        <div class="mb-3">
                            <label for="codigo" class="form-label">Código</label>
                            <input type="text" class="form-control" id="codigo" name="codigo" placeholder="Ex: 1.1.1" autocomplete="off" value="<?=$conta->codigo ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="nome" class="form-label">Nome</label>
                            <input type="text" class="form-control" id="nome" name="nome" placeholder="Nome da conta" autocomplete="off" value="<?=$conta->nome ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-3">
                        <div class="mb-3">
                            <label for="tipo" class="form-label">Tipo</label>
                            <select name="tipo" id="tipo" class="form-select" data-search="false">
                                <option value="DESPESA" <?=(isset($conta->tipo) && $conta->tipo === "DESPESA") ? "selected" : ""?>>Despesa</option>
                                <option value="RECEITA" <?=(isset($conta->tipo) && $conta->tipo === "RECEITA") ? "selected" : ""?>>Receita</option>
                            </select>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-12">
                        <div class="mb-3">
                            <label for="descricao" class="form-label">Descrição</label>
                            <textarea class="form-control" id="descricao" name="descricao" rows="2" placeholder="Descrição opcional"><?=$conta->descricao ?? ""?></textarea>
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection">
                <div class="crud-section-copy mb-3">
                    <h5>Situação</h5>
                </div>
                <div class="row">
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="ativo" class="form-label">Ativo</label>
                            <select name="ativo" id="ativo" class="form-select" data-search="false">
                                <option value="1" <?=(!isset($conta->ativo) || (int) $conta->ativo === 1) ? "selected" : ""?>>Sim</option>
                                <option value="0" <?=isset($conta->ativo) && (int) $conta->ativo === 0 ? "selected" : ""?>>Não</option>
                            </select>
                        </div>
                    </div>
                </div>
            </div>

            <div class="mt-3 pt-4 border-top">
                <div class="d-grid gap-2 d-sm-block">
                    <button class="btn btn-primary" type="submit">Salvar</button>
                    <a href="<?=$router->route("admin.financeiro.plano.conta.index")?>" class="btn btn-white"><i class="uil uil-left"></i> Voltar</a>
                </div>
            </div>
        </div>
    </div>
</form>
