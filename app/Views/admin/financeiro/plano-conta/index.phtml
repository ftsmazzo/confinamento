<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-buttons") ?>
    <?php if (!empty($permissao["inserir"])) : ?>
        <a class="btn btn-primary" href="<?=$router->route("admin.financeiro.plano.conta.novo")?>">
            <i class="fa fa-plus fa-fw"></i> Nova Conta
        </a>
    <?php endif ?>
<?php $this->end() ?>

<div class="card crud-table-card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover table-sm table-datatable table-datatable-alt crud-table">
                <thead>
                    <tr>
                        <th>Código</th>
                        <th>Nome</th>
                        <th>Tipo</th>
                        <th>Ativo</th>
                        <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                            <th class="text-end actions-column"></th>
                        <?php endif ?>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($dados as $d) : ?>
                        <tr>
                            <td>
                                <?php if ($permissao["editar"]) : ?>
                                    <a class="edit" href="<?=$router->route("admin.financeiro.plano.conta.editar", ["id" => $d->hash])?>">
                                        <?=$d->codigo?>
                                    </a>
                                <?php else : ?>
                                    <span><?=$d->codigo?></span>
                                <?php endif ?>
                            </td>
                            <td><?=$d->nome?></td>
                            <td>
                                <?php $tipoClass = $d->tipo === "RECEITA" ? "bg-success" : "bg-danger"; ?>
                                <span class="badge <?=$tipoClass?>"><?=\App\Models\Financeiro\PlanoConta::tipoLabel($d->tipo)?></span>
                            </td>
                            <td><?=!empty($d->ativo) ? '<span class="badge filled-outlined bg-success">Sim</span>' : '<span class="badge filled-outlined bg-secondary">Não</span>'?></td>
                            <?php if ($permissao["editar"] || $permissao["excluir"]) : ?>
                                <td class="text-end actions-column">
                                    <div class="table-actions">
                                        <?php if ($permissao["editar"]) : ?>
                                            <a class="table-action-btn" data-tooltip="true" title="Editar" href="<?=$router->route("admin.financeiro.plano.conta.editar", ["id" => $d->hash])?>">
                                                <i class="uil-edit-alt"></i>
                                            </a>
                                        <?php endif ?>
                                        <?php if ($permissao["excluir"]) : ?>
                                            <button type="button" class="table-action-btn table-action-danger" data-tooltip="true" title="Excluir" onclick="Delete('financeiro/plano-conta/delete', '<?=$d->hash?>')">
                                                <i class="uil uil-trash"></i>
                                            </button>
                                        <?php endif ?>
                                    </div>
                                </td>
                            <?php endif ?>
                        </tr>
                    <?php endforeach ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
