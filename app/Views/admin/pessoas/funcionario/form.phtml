<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
<?php $this->end() ?>

<form id="form-pessoas-funcionario" class="form-validate" action="<?=$url_action?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf?>">
    <?php if (!empty($funcionario->id)) : ?>
        <input type="hidden" name="id" value="<?=$funcionario->hash()?>">
    <?php endif ?>

    <div class="card crud-form-card">
        <div class="card-body">
            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Identificação</h5>
                    <small>Dados básicos do funcionário.</small>
                </div>

                <div class="row">
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="nome" class="form-label">Nome</label>
                            <input type="text" class="form-control" id="nome" name="nome" placeholder="Nome completo" autocomplete="off" value="<?=$funcionario->nome ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-3">
                        <div class="mb-3">
                            <label for="cpf" class="form-label">CPF</label>
                            <input type="text" class="form-control input-cpf" id="cpf" name="cpf" placeholder="000.000.000-00" autocomplete="off" value="<?=$funcionario->cpf ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-3">
                        <div class="mb-3">
                            <label for="id_unidade" class="form-label">Unidade</label>
                            <select name="id_unidade" id="id_unidade" class="form-select select2" data-search="true">
                                <option value="">Selecione</option>
                                <?php foreach ($unidades as $unidade) : ?>
                                    <option value="<?=$unidade->id?>" <?=isset($funcionario->id_unidade) && (int) $funcionario->id_unidade === (int) $unidade->id ? "selected" : ""?>><?=$unidade->nome?></option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Função</h5>
                    <small>Cargo, setor e período de trabalho.</small>
                </div>

                <div class="row">
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="cargo" class="form-label">Cargo</label>
                            <input type="text" class="form-control" id="cargo" name="cargo" placeholder="Ex: Vaqueiro, Veterinário, Gerente" autocomplete="off" value="<?=$funcionario->cargo ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="setor" class="form-label">Setor</label>
                            <select name="setor" id="setor" class="form-select" data-search="false">
                                <option value="">Selecione</option>
                                <?php foreach (["OPERACIONAL" => "Operacional", "TECNICO" => "Técnico", "ADMINISTRATIVO" => "Administrativo", "APOIO" => "Apoio"] as $valor => $label) : ?>
                                    <option value="<?=$valor?>" <?=isset($funcionario->setor) && $funcionario->setor === $valor ? "selected" : ""?>><?=$label?></option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="id_usuario" class="form-label">Usuário do Sistema (opcional)</label>
                            <select name="id_usuario" id="id_usuario" class="form-select select2" data-search="true">
                                <option value="">Nenhum</option>
                                <?php foreach ($usuarios as $usuario) : ?>
                                    <option value="<?=$usuario->id?>" <?=isset($funcionario->id_usuario) && (int) $funcionario->id_usuario === (int) $usuario->id ? "selected" : ""?>><?=$usuario->nome?></option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="data_admissao" class="form-label">Data de Admissão</label>
                            <input type="date" class="form-control" id="data_admissao" name="data_admissao" autocomplete="off" value="<?=!empty($funcionario->data_admissao) ? dateus($funcionario->data_admissao) : ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="data_demissao" class="form-label">Data de Demissão</label>
                            <input type="date" class="form-control" id="data_demissao" name="data_demissao" autocomplete="off" value="<?=!empty($funcionario->data_demissao) ? dateus($funcionario->data_demissao) : ""?>">
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Contato</h5>
                    <small>Telefone e e-mail do funcionário.</small>
                </div>

                <div class="row">
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="telefone" class="form-label">Telefone</label>
                            <input type="text" class="form-control input-fone" id="telefone" name="telefone" placeholder="Telefone" autocomplete="off" value="<?=$funcionario->telefone ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="email" class="form-label">E-mail</label>
                            <input type="email" class="form-control" id="email" name="email" placeholder="E-mail" autocomplete="off" value="<?=$funcionario->email ?? ""?>">
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Observações</h5>
                    <small>Informações livres para apoio operacional.</small>
                </div>

                <div class="row">
                    <div class="col-12">
                        <div class="mb-3">
                            <textarea class="form-control" id="observacao" name="observacao" rows="3" placeholder="Observação"><?=$funcionario->observacao ?? ""?></textarea>
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection">
                <div class="crud-section-copy mb-3">
                    <h5>Situação</h5>
                    <small>Defina se o funcionário está ativo.</small>
                </div>

                <div class="row">
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="ativo" class="form-label">Ativo</label>
                            <select name="ativo" id="ativo" class="form-select" data-search="false">
                                <option value="1" <?=(!isset($funcionario->ativo) || (int) $funcionario->ativo === 1) ? "selected" : ""?>>Sim</option>
                                <option value="0" <?=isset($funcionario->ativo) && (int) $funcionario->ativo === 0 ? "selected" : ""?>>Não</option>
                            </select>
                        </div>
                    </div>
                </div>
            </div>

            <div class="mt-3 pt-4 border-top">
                <div class="d-grid gap-2 d-sm-block">
                    <button class="btn btn-primary" type="submit">Salvar</button>
                    <a href="<?=$router->route("admin.pessoas.funcionario.index")?>" class="btn btn-white"><i class="uil uil-left"></i> Voltar</a>
                </div>
            </div>
        </div>
    </div>
</form>
