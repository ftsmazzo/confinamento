<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
<?php $this->end() ?>

<form id="form-confinamento-local-estoque" class="form-validate" action="<?=$url_action?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf?>">
    <?php if (!empty($local->id)) : ?>
        <input type="hidden" name="id" value="<?=$local->hash()?>">
    <?php endif ?>

    <div class="card crud-form-card">
        <div class="card-body">
            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Identificação</h5>
                    <small>Dados básicos do local de estoque.</small>
                </div>

                <div class="row">
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="local-nome" class="form-label">Nome</label>
                            <input type="text" class="form-control" id="local-nome" name="nome" placeholder="Nome do local" autocomplete="off" value="<?=$local->nome ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="local-codigo" class="form-label">Código</label>
                            <input type="text" class="form-control" id="local-codigo" name="codigo" placeholder="Código" autocomplete="off" value="<?=$local->codigo ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="local-unidade" class="form-label">Unidade</label>
                            <select name="id_unidade" id="local-unidade" class="form-select" data-search="false">
                                <option value="">Selecione</option>
                                <?php foreach (($unidades ?? []) as $unidade) : ?>
                                    <option value="<?=$unidade->id?>" <?=isset($local->id_unidade) && (int) $local->id_unidade === (int) $unidade->id ? "selected" : ""?>><?=$unidade->nome?> (<?=$unidade->codigo?>)</option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-3">
                        <div class="mb-3">
                            <label for="local-tipo" class="form-label">Tipo</label>
                            <input type="text" class="form-control" id="local-tipo" name="tipo" placeholder="Tipo" autocomplete="off" value="<?=$local->tipo ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-3">
                        <div class="mb-3">
                            <label for="local-capacidade" class="form-label">Capacidade</label>
                            <input type="text" class="form-control" id="local-capacidade" name="capacidade" placeholder="Capacidade" autocomplete="off" value="<?=$local->capacidade ?? ""?>">
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Situação</h5>
                    <small>Responsável e status do local.</small>
                </div>

                <div class="row">
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="local-responsavel" class="form-label">Responsável</label>
                            <input type="text" class="form-control" id="local-responsavel" name="responsavel" placeholder="Responsável" autocomplete="off" value="<?=$local->responsavel ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="local-ativo" class="form-label">Ativo</label>
                            <select name="ativo" id="local-ativo" class="form-select" data-search="false">
                                <option value="1" <?=(!isset($local->ativo) || (int) $local->ativo === 1) ? "selected" : ""?>>Sim</option>
                                <option value="0" <?=isset($local->ativo) && (int) $local->ativo === 0 ? "selected" : ""?>>Não</option>
                            </select>
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection">
                <div class="crud-section-copy mb-3">
                    <h5>Observações</h5>
                    <small>Informações livres para apoio operacional.</small>
                </div>

                <div class="row">
                    <div class="col-12">
                        <div class="mb-3">
                            <textarea class="form-control" id="local-observacao" name="observacao" rows="5" placeholder="Observação"><?=$local->observacao ?? ""?></textarea>
                        </div>
                    </div>
                </div>
            </div>

            <div class="mt-3 pt-4 border-top">
                <div class="d-grid gap-2 d-sm-block">
                    <button class="btn btn-primary" type="submit">Salvar</button>
                    <a href="<?=$router->route("admin.confinamento.local.estoque.index")?>" class="btn btn-white"><i class="uil uil-left"></i> Voltar</a>
                </div>
            </div>
        </div>
    </div>
</form>
