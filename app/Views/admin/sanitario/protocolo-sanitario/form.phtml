<?php $this->layout("app/template/base"); ?>

<?php $this->start("page-actions") ?>
<?php $this->end() ?>

<form id="form-sanitario-protocolo" class="form-validate" action="<?=$url_action?>" method="POST">
    <input type="hidden" name="csrf" value="<?=$csrf?>">
    <?php if (!empty($protocolo->id)) : ?>
        <input type="hidden" name="id" value="<?=$protocolo->hash()?>">
    <?php endif ?>

    <div class="card crud-form-card">
        <div class="card-body">
            <div class="crud-subsection mb-4">
                <div class="crud-section-copy mb-3">
                    <h5>Identificação</h5>
                    <small>Protocolo padrão de vacinação, vermifugação ou outro tratamento sanitário.</small>
                </div>

                <div class="row">
                    <div class="col-12 col-md-6">
                        <div class="mb-3">
                            <label for="nome" class="form-label">Nome</label>
                            <input type="text" class="form-control" id="nome" name="nome" placeholder="Ex: Vacinação Febre Aftosa" autocomplete="off" value="<?=$protocolo->nome ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12 col-md-3">
                        <div class="mb-3">
                            <label for="id_produto_estoque" class="form-label">Produto Padrão</label>
                            <select name="id_produto_estoque" id="id_produto_estoque" class="form-select select2" data-search="true">
                                <option value="">Selecione</option>
                                <?php foreach ($produtos as $produto) : ?>
                                    <option value="<?=$produto->id?>" <?=isset($protocolo->id_produto_estoque) && (int) $protocolo->id_produto_estoque === (int) $produto->id ? "selected" : ""?>><?=$produto->nome?></option>
                                <?php endforeach ?>
                            </select>
                        </div>
                    </div>
                    <div class="col-12 col-md-3">
                        <div class="mb-3">
                            <label for="dias_carencia_padrao" class="form-label">Carência Padrão (dias)</label>
                            <input type="number" min="0" class="form-control" id="dias_carencia_padrao" name="dias_carencia_padrao" placeholder="Ex: 21" autocomplete="off" value="<?=$protocolo->dias_carencia_padrao ?? ""?>">
                        </div>
                    </div>
                    <div class="col-12">
                        <div class="mb-3">
                            <label for="descricao" class="form-label">Descrição</label>
                            <textarea class="form-control" id="descricao" name="descricao" rows="3" placeholder="Descrição"><?=$protocolo->descricao ?? ""?></textarea>
                        </div>
                    </div>
                </div>
            </div>

            <div class="crud-subsection">
                <div class="crud-section-copy mb-3">
                    <h5>Situação</h5>
                    <small>Defina se o protocolo está ativo.</small>
                </div>

                <div class="row">
                    <div class="col-12 col-md-4">
                        <div class="mb-3">
                            <label for="ativo" class="form-label">Ativo</label>
                            <select name="ativo" id="ativo" class="form-select" data-search="false">
                                <option value="1" <?=(!isset($protocolo->ativo) || (int) $protocolo->ativo === 1) ? "selected" : ""?>>Sim</option>
                                <option value="0" <?=isset($protocolo->ativo) && (int) $protocolo->ativo === 0 ? "selected" : ""?>>Não</option>
                            </select>
                        </div>
                    </div>
                </div>
            </div>

            <div class="mt-3 pt-4 border-top">
                <div class="d-grid gap-2 d-sm-block">
                    <button class="btn btn-primary" type="submit">Salvar</button>
                    <a href="<?=$router->route("admin.sanitario.protocolo.index")?>" class="btn btn-white"><i class="uil uil-left"></i> Voltar</a>
                </div>
            </div>
        </div>
    </div>
</form>
